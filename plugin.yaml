name: hermes-agent-a2a
version: "3.0.0"
description: |
  A2A (Agent-to-Agent) protocol plugin for Hermes Agent.
  Self-contained: exposes a2a_telegram, a2a_call, a2a_discover, a2a_list tools.
  No gateway patch required. Fleet-agnostic — no hardcoded paths.
requires_env: []
